        ## Run instructions

Backend:
1. Configure `backend/src/main/resources/application.properties` if needed.
2. Run `mvn clean package` then `mvn spring-boot:run`.

Frontend:
1. cd frontend
2. npm install
3. npm start

Or use docker-compose to run MySQL first.
